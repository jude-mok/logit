# Leave empty to use the same-origin /api proxy in Vite and Vercel.
VITE_API_URL=
# Optional local backend override; defaults to the deployed Railway API.
API_PROXY_TARGET=https://logit-production.up.railway.app
